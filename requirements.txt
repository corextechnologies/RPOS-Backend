fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
email-validator==2.2.0

# Timezone database for zoneinfo. Windows ships no system tz database at all, and
# slim Linux images often drop theirs, so a branch's timezone (Asia/Karachi) can
# only be resolved if this is installed — see app/services/business_day.py.
tzdata==2026.1

# Hijri <-> Gregorian conversion for the Phase 7 event calendar. The Islamic
# calendar drifts ~10-11 days earlier each Gregorian year, so Ramadan and both
# Eids must be computed per year rather than stored as dates — see
# app/services/hijri.py. Note it computes the tabular Umm al-Qura date; Pakistan
# starts on a local moon sighting, so results are estimates the Admin confirms.
hijridate==2.6.0

# Image storage (Cloudflare R2, S3-compatible) + shrinking on upload
boto3==1.43.57
botocore==1.43.57
s3transfer==0.19.2
jmespath==1.1.0
pillow==12.3.0

# Dev / test
pytest==8.3.4
httpx==0.28.1